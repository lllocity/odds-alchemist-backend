# タスク: Step 12の完了（データマッピング修正とリファクタリング）

## 現状の課題
Google Sheetsへのデータ保存処理までは開通したが、以下の問題がある。
1. スプレッドシートの列ヘッダーと、実際に書き込まれるデータの列がズレている。
2. スポナビのHTMLからデータを抽出する `RaceOddsParser.java` が、度重なるデバッグにより場当たり的なテキスト判定ロジックになっている。

## 実行してほしいこと
以下の3点を行い、Step 12を完了させてください。

### 1. キーと値のマッピング修正
`OddsSyncService.java` または関連するDTOを修正し、Google Sheetsに以下の順序で正しくデータが書き込まれるように配列の格納順を修正してください。
- A列: 取得タイムスタンプ (yyyy/MM/dd HH:mm:ss)
- B列: レース名
- C列: 馬番 
- D列: 馬名
- E列: 単勝オッズ
- F列: 複勝オッズ（下限）
- G列: 複勝オッズ（上限）

### 2. `RaceOddsParser` のリファクタリング
現在のテキスト判定ロジックを整理し、より読みやすく保守性の高いコードにリファクタリングしてください。スポナビのHTML構造（`https://sports.yahoo.co.jp/keiba/race/odds/tfw/...`）から確実にデータを抽出できる堅牢性を担保してください。不要なデバッグコードは削除して構いません。

### 3. テストコードの修正・追加
リファクタリングした `RaceOddsParser` が正しく機能するかを検証するJUnitテストコード（`RaceOddsParserTest.java` 等）を作成または修正してください。ダミーのHTML文字列を渡し、期待通りの `OddsData` リストが返ることをアサートしてください。